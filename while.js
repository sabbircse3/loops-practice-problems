// if (conditions) {

// }

/**
 * 1. loop variable
 * 2. condition 
 * 3. loop body
 * 4. change loop variable
 */

var roastGiven = 0;
while (roastGiven < 7) { 
    console.log('Roast Den, Please!');
    // roastGiven = roastGiven + 1;
    // roastGiven += 1;
    console.log(roastGiven);
    roastGiven++;
}